<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="first.dao.BrandMapper">
    <select id="SelectBrand" resultType="first.pojo.Brand">
        select * from brand limit 11
    </select>
    <select id="SelectCarName" resultType="first.pojo.Details">
        select caname from details limit 11
    </select>

</mapper>